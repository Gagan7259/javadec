package Stream;

import java.io.IOException;
import java.nio.file.Files;
import java.nio.file.Paths;
import java.util.stream.Stream;

public class jsonfilereader {

	public static void main(String[] args) throws IOException {
		/*
		 * // Read the JSON file into a stream Stream<String> stream =
		 * Files.lines(Paths.get("file.json"));
		 * 
		 * // Parse the stream into a stream of JSON objects Stream<Object> jsonStream =
		 * stream.map(line -> new Gson().fromJson(line, JsonObject.class));
		 * 
		 * // Iterate over the stream and print each JSON object
		 * jsonStream.forEach(System.out::println);
		 */
	  }
}
